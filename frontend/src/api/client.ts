// src/api/client.ts — 后端 API 封装
// - uploadPDF(file) → POST /api/report/upload
// - getStatus(sid) → GET /api/report/status/{sid}
// - getReport(rid) → GET /api/report/{rid}
// - downloadReport(rid) → GET /api/report/download/{rid}
// - chat(sid, question) → POST /api/report/chat
